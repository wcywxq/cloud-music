import React from "react";
import { ProfileItemProps } from "@/pages/profile";

const App: React.FC<ProfileItemProps> = props => {
  // const { data, loading } = props;

  return <>相似歌手</>;
};

export const ProfileSimilar = App;
